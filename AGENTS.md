# 燃赛路跑项目协作约定

## 项目结构与技术栈

- `back/`：RuoYi-Vue-Plus 5.6.2 单体后端，Spring Boot、Maven、JDK 17。
- `front/`：HBuilderX 创建的 uni-app Vue 3 微信小程序项目；当前没有 `package.json`，不要按普通 npm/Vite 项目处理。
- `docs/交接文档.md`：产品边界、详细需求和架构决策。
- `CODEX_PROGRESS.md`：长期目标、已完成证据、当前断点和下一步。
- `back/script/sql/running/`：燃赛路跑顺序数据库迁移。

开始工作前先阅读 `CODEX_PROGRESS.md` 与 `docs/交接文档.md`，以其中最新明确约束为准。

## 不可违背的约束

- **Cloud 只能使用专属分支 `codex/cloud-running`。** 在确认当前分支恰好是 `codex/cloud-running` 后，Cloud 任务完成并验证通过时可以直接 `commit` 并 `push` 到同名远端分支。严禁直接修改、提交或推送 `main`，严禁强推，严禁未经负责人明确指令创建/更新 Pull Request、合并到主分支或发布；若当前分支不是专属分支，必须停止提交并报告。
- 后端只做单体应用，运行和编译统一使用 JDK 17，不拆微服务。
- 地图统一使用腾讯地图/腾讯位置服务，轨迹坐标统一为 GCJ-02。
- `lupao/` 是老板 AI 视觉样稿。负责人已明确授权在 `codex/cloud-running` 中提交一次冻结的参考快照；该快照只读、不是正式源码或运行依赖，正式实现仍只能落在 `front/`。
- **一次性快照提交后，任何任务都严禁修改、格式化、删除、重命名、暂存或再次提交 `lupao/` 下的已有文件。** 根目录 `.gitignore` 继续用于阻止新增未跟踪文件，但 Git 不会忽略已经纳入版本管理的文件，因此提交前还必须显式检查暂存清单中不存在任何后续 `lupao/` 变更。
- Cloud 可读取已提交的冻结快照作视觉对照，但不得复制其中的 Mock 数据或把它作为业务规则依据，也不得在无法运行 HBuilderX/微信开发者工具时声称完成了 1:1 真机验收。
- `20260903_running_v1.sql` 至 `20260904_running_v7.sql` 均已执行且不可修改。数据库变化只能新增下一顺序迁移，当前从 `v8` 开始；禁止覆盖、重命名、重排或重复执行旧迁移。
- 执行新迁移前先读取 `run_schema_history`，核对已登记版本；迁移文件存在不等于已经执行。
- 当前开发阶段按负责人要求使用工程内明文开发配置，不主动改成环境变量。不得在回复、日志或新文档中复制真实密码、AppSecret、短信密钥或地图 Key；部署前再统一迁移至安全注入并轮换密钥。
- 手机号是平台用户唯一归并身份。微信一键登录必须同时使用微信登录 code 与官方手机号授权 phoneCode，并按服务端验证出的完整手机号归并；不得新增登录后手工绑定手机号流程。
- MySQL 是业务事实来源；Redis 只能承载登录态、短缓存、幂等和排行榜加速。

## 修改与验证

- 只修改当前任务所需文件，保留工作区中已有且无关的用户改动。
- Cloud 在专属分支提交时也不使用 `git add -A`；必须逐项暂存已审核文件，并确认 `git diff --cached --name-only` 中不存在任何后续 `lupao/` 变更。任何疑似密钥、密码、AppSecret、短信凭据或地图 Key 都不得提交。
- 后端常用验证命令：

  ```bash
  cd back
  mvn -B -ntp -DskipTests compile
  ```

- 需要完整打包时：

  ```bash
  cd back
  mvn -B -ntp -DskipTests package
  ```

- Cloud 中可做 Java 编译、单元测试、SQL 静态检查和前端源码检查；不能把“无法启动 HBuilderX/微信开发者工具或缺少本地 MySQL、Redis、真实微信能力”误报为代码失败。
- HBuilderX 编译、微信模拟器、真机定位、微信手机号授权、短信送达、腾讯逆地址解析和真实业务接口必须分别记录验证证据，不能互相替代。
- API 验收必须同时核对 HTTP 状态和响应 JSON 的 `code=200`；编译通过不能代替运行验证。

## 工作完成后的记录

- 每个完整支点完成后更新 `CODEX_PROGRESS.md`：实际改动、验证命令/结果、未验证项和下一步。
- 如果新增或调整需求边界，同时更新 `docs/交接文档.md`。
- 不得把静态页面、Mock 数据、SQL 文件或未经真机验证的能力描述成已正式完成。
