# 燃赛路跑开发进度

## 1. 总目标

以 `lupao/` 中老板确认方向的 AI 视觉原型为重点参考，在 `front/` 使用 HBuilderX + uni-app（Vue 3）开发微信小程序，在 `back/` 使用 RuoYi-Vue-Plus 5.6.2 + JDK 17 开发单体后端，接入 MySQL、Redis、微信登录与腾讯地图，完成“开始运动—实时记录—结束保存—统计排行—成就激励”的可用闭环。

本文档是 Codex 的持续工作台。每完成一个阶段，都必须更新任务状态、实际改动、验证证据、遗留问题和下一步；不能把静态页面、编译通过或 Mock 数据当作真实业务完成。

## 2. 不可违背的项目约束

- `lupao/` 是老板用 AI 生成并要求重点参考的视觉样稿，首页、线路卡片、运动中卡路里主视觉、国风路线、勋章和个人中心应尽量保持其视觉语言与信息层级。
- `lupao/` 不是正式源码和业务规则依据；正式代码只能写入 `front/`、`back/`、`docs/` 等受版本管理的目录。
- 负责人已授权仅在 `codex/cloud-running` 提交一次 `lupao/` 冻结视觉参考快照；快照提交后禁止再修改、暂存或提交其中任何已有文件。`.gitignore` 继续阻止新增未跟踪文件，但提交前仍须检查暂存清单没有后续 `lupao/` 变更。
- 后端为单体应用，不拆微服务；运行时使用 JDK 17。
- 地图统一使用腾讯地图/腾讯位置服务；轨迹坐标统一采用可直接用于微信地图组件的 GCJ-02。
- 当前正式前端不能复制 Mock 数据冒充真实数据；业务页面必须以真实接口、真实空态、加载态、错误态和权限拒绝态为准。
- 当前本地开发阶段，数据库、Redis、微信、短信和腾讯地图相关配置直接明文写入工程配置文件，不使用 `RUNNING_*` 环境变量；部署前必须统一迁回环境变量或密钥管理并轮换已暴露的真实密钥。
- `20260903_running_v1.sql` 至 `20260904_running_v7.sql` 已在本地执行，现均视为不可变迁移；任何后续表结构或初始化数据变化必须新增 `v8` 等顺序迁移文件，禁止回改或重复执行已执行版本。
- Codex Cloud 网页任务的源分支只能选择 `codex/cloud-running`。容器内临时分支名为 `work`、不暴露 `origin` 属正常隔离机制，不得因此停止；应继续开发、验证并在任务内逐项审核后提交。只有容器确实提供远程时才可非强制推送到 `codex/cloud-running`，否则由负责人稍后同步任务内提交。严禁改动/推送 `main`、强推、未经指令开 PR、合并主分支或发布。

## 3. 当前基线

### 已完成

- [x] 引入 RuoYi-Vue-Plus 5.6.2，JDK 17 Maven 编译通过。
- [x] 本地 MySQL、Redis 与 `DromaraApplication` 可启动。
- [x] 关闭当前不使用的 Monitor Admin 和 SnailJob 客户端连接，源码暂时保留。
- [x] 新增公开连通性接口 `GET /app/ping`。
- [x] `front/` 新增统一 API 地址与 `uni.request` 请求封装，首页可显示后端连接状态。
- [x] `lupao/` 保存视觉原型；按负责人要求在云端分支提交一次只读快照，后续新增文件持续忽略、已有文件严禁再次提交变更。
- [x] 新增单体业务模块 `ruoyi-running`，并接入 Maven 聚合与 `ruoyi-admin`。
- [x] 新增首版用户运动档案模型、接口和建表 SQL；`v1` 已由负责人在本地 MySQL 执行并核对 13 张业务表。
- [x] 新增微信官方手机号授权一键登录：同时提交微信身份 code 与 phoneCode，以验证手机号归并平台用户并签发 Sa-Token；尚缺真实 AppSecret/主体能力联调。
- [x] 新增手机号验证码登录和双入口登录页面；与微信入口共用手机号主身份。真实短信已能送达；首次登录在用户实体自动转换处失败，现已修复并待重启后复验。
- [x] 新增真实用户档案页和登录方式自动关联状态查询；不再提供登录后手工绑定手机号；尚缺数据库与真机联调。
- [x] 新增单次 GCJ-02 定位页、微信地图展示和腾讯位置服务后端逆地址解析代理；尚缺 Key 与真机联调。
- [x] 新增正式积分账户、每日签到、线路节点/成就奖励和不可重复积分流水；前端积分页已接入真实接口。
- [x] 排行榜按每次运动创建时的常住省市快照归属，修改档案不再搬动历史成绩。

### 尚未完成

- [ ] 双登录完整联调、档案页运行验证和正式隐私协议内容；短信发送已验证，短信登录修复待 IDEA 重启后复验，微信授权仍需真机验证。
- [ ] 腾讯地图 Key、定位页运行验证和真机定位验收；代码与权限声明已完成。
- [x] 跑步运动状态机、前台轨迹采集、实时配速/距离/卡路里估算、批量上传与服务端结算代码；数据库、HBuilderX 和真机运行仍待验证。
- [ ] 运动记录/统计、文化线路、城市足迹、成就、地区排行榜和非跑步运动代码已实现；完整业务运行验证尚未完成。
- [ ] 正式页面还原、异常状态、自动化测试和真机验收。

## 4. 总体技术方案

### 4.1 前端

- HBuilderX、uni-app Vue 3、微信小程序目标端。
- `front/common/config.js` 管理环境 API 地址；开发者工具使用 `127.0.0.1`，真机使用局域网地址，生产使用微信后台配置的 HTTPS 合法域名。
- `front/common/request.js` 统一处理超时、业务状态码、Authorization、登录失效和错误提示。
- 使用 uni-app/微信小程序原生 `map` 组件展示当前位置和 `polyline` 轨迹，不在运动期间持续请求地图服务。
- 定位采集优先使用小程序位置更新能力；前后台持续定位必须经过类目权限、接口申请、隐私声明和真机测试后才能确认上线。
- 运动过程在本地分段缓存轨迹，按批次上传；弱网时保留待上传批次，恢复网络后补传。

### 4.2 后端

- 已在现有单体工程中增加 `ruoyi-running` 业务模块，由 `ruoyi-admin` 一并打包和启动，不新增独立服务。
- Sa-Token 承载业务登录态；微信 `code2Session` 只由后端调用，开发期 AppSecret 仅保存在后端配置文件，部署前改为安全注入。
- 小程序令牌写入本地存储，后续请求统一携带 `Authorization: Bearer <token>` 和固定 `clientid=running-miniprogram`；收到 401 时清除失效令牌。
- 身份映射当前只保存 AppID、`openid/unionid` 或手机号的 HMAC-SHA-256 摘要和平台用户 ID；摘要由至少 32 位的服务端稳定密钥生成，不保存微信 `session_key`，也不向小程序返回原始身份标识。
- MySQL 保存用户、运动摘要、轨迹批次/轨迹点、统计、足迹、成就及排行榜数据。
- Redis 用于登录态、短期缓存、排行榜加速、幂等与必要的并发控制；MySQL 始终是业务事实来源。
- 所有运动结算必须由服务端重新校验距离、时长、速度、轨迹时间顺序和卡路里范围，不能直接相信客户端汇总值。

### 4.3 腾讯地图

- 微信地图组件负责地图渲染、标记点和轨迹折线；定位坐标使用 `gcj02`。
- 腾讯位置服务用于逆地址解析、行政区识别、POI/地址能力和后续可能的路线服务。
- 腾讯位置服务服务端 Key 在开发期放入后端配置文件，由后端代理逆地址解析；不得下发小程序，部署前改为安全注入。
- 如小程序端必须使用腾讯位置服务 JS SDK，应创建绑定微信小程序 AppID、限制调用来源和 API 范围的独立 Key，不与服务端 Key 共用。
- 数据库保存经纬度、定位时间、精度、速度、方向及轨迹点序号；城市足迹保存腾讯行政区划码和当时解析结果，避免城市名称变化造成统计不可审计。
- 逆地址解析不对每个轨迹点调用：开始点、结束点、城市边界候选点或结算时抽样调用，控制额度和延迟。

## 5. 分阶段实现步骤

### 阶段 0：工程基线与连通性

- [x] 前后端 Ping 链路。
- [x] 将本地地址、生产域名、微信 AppID、微信 AppSecret、腾讯地图 Key 集中到配置入口；当前开发期按要求使用明文配置，部署前迁回环境变量。
- [x] 增加本地明文配置说明及部署前迁移/密钥轮换提醒。
- [x] 增加统一后端业务模块目录；小程序业务接口统一使用 `/app/**`，继续复用框架 `R<T>` 异常响应。

验收：开发者工具首页实际收到 `code=200` 且 `data.status=UP`；不是只验证 HTTP 200。

### 阶段 1：双登录方式、手机号归并与用户档案

- [x] 实现参考 `lupao/` 橙色品牌视觉的双入口登录页，封装 `uni.login`、手机号验证码请求、令牌本地存储、退出及失效令牌清理。
- [x] 微信按钮接入 `open-type="getPhoneNumber"`，后端分别消费 `uni.login` code 和手机号 phoneCode，以微信官方验证手机号查找/创建平台用户并签发 Sa-Token；尚未配置真实 AppSecret 和手机号能力做运行验证。
- [x] 实现小程序退出登录及前端失效令牌清理。
- [x] 实现手机号验证码发送、验证码一次性校验和手机号登录；尚未配置真实短信供应商做运行验证。
- [x] 账号归并规则修订为“完整且已验证的手机号是平台主身份”：先短信后微信、先微信后短信均自动进入同一个用户，不提供登录后手工绑定流程。
- [x] `openid/unionid` 只作为附加微信登录凭证；昵称、头像、设备、尾号和地理位置均不作为账号归并依据。
- [x] 实现用户档案实体、参数校验、查询/保存服务和受登录保护的接口。
- [x] 实现用户档案前端页面，接入昵称、性别、生日、身高、体重、常住省市和登录方式自动关联状态；尚未运行联调。
- [x] 增加当前用户登录方式状态接口，只返回是否已自动关联，不返回原始手机号、openid 或 unionid。
- [x] 产出 `run_user_profile`、`run_wechat_identity`、`run_phone_identity` 首版建表 SQL，分别约束档案、微信和手机号身份唯一性。
- [x] 在本地 `running` 数据库执行 v1-v7，并验证当前业务表、字段、索引和迁移登记。
- [x] 增加头像选择、图片类型/尺寸/大小校验和正式对象存储上传，档案保存对象存储 ID 与正式 URL；普通档案保存不再接受客户端任意头像 URL。对象存储真实上传仍待重启后以登录用户验证。
- [x] 新增可独立阅读和互相切换的用户协议/隐私政策页面，登录勾选与协议链接不再混为一个点击动作；研发验收文本明确手机号归并、运动估算、定位、第三方处理与用户权利边界。
- [x] 统一处理接口和文件上传的 HTTP/业务 `401`：清除令牌及账号绑定的未上传轨迹缓存，并将并发失效响应收敛为一次提示和一次登录页跳转；重新登录后可再次正确处理失效。
- [ ] 上线前由实际运营主体/法律人员确认主体名称、联系渠道、保存期限和未成年人规则，并实现账号注销/个人信息申请入口；定位拒绝状态和登录失效跳转的微信真机表现仍待验证。

已完成代码接口：`POST /app/auth/wechat-login`、`POST /app/auth/sms-code`、`POST /app/auth/phone-login`、`POST /app/auth/logout`、`GET /app/user/login-methods`、`GET /app/user/profile`、`PUT /app/user/profile`。微信登录请求必须同时包含用途不同的 `code` 与 `phoneCode`；后端不接受客户端直接上报手机号或 openid 作为微信登录事实。微信和手机号身份标识不出现在登录响应中。上述接口仍需完成数据库迁移、真实微信/短信配置后的运行联调。

验收：首次登录建档、再次登录复用用户、Token 失效、拒绝授权和资料更新均有真实验证。

### 阶段 2：腾讯地图与定位能力验证

- [ ] 申请腾讯位置服务应用与 Key，配置微信 AppID 白名单/API 权限。
- [x] 配置小程序 `scope.userLocation` 用途声明和 `getLocation` 私有信息声明；正式隐私协议仍需上线前补齐。
- [x] 实现定位授权页、单次高精度 GCJ-02 定位、权限拒绝引导和微信原生地图当前位置展示；尚未真机运行。
- [x] 后端代理腾讯逆地址解析并返回省、市、区及行政区划码；开发期 Key 从后端配置文件读取，并以约 10 米网格缓存地址 10 分钟。
- [ ] 在至少一台真机验证定位精度、拒绝后恢复授权、前后台切换和不同网络环境。

已完成代码接口：`POST /app/map/reverse-geocode`。接口要求登录态，坐标范围由服务端校验，每 IP 每分钟最多 30 次；腾讯服务端 Key 不下发前端，异常日志不打印 Key 或完整请求 URL。

验收：开发者工具模拟定位和真机定位都能显示；真实设备结果与腾讯地图基本一致；Key 不出现在 Git 历史。

### 阶段 3：跑步记录核心闭环

- [x] 定义并实现首版状态机：页面 `IDLE/LOCATING/RUNNING/PAUSED/FINISHING/COMPLETED`，服务端持久化 `RUNNING/PAUSED/COMPLETED/FAILED`；增加明确二次确认的“放弃本次记录”，服务端幂等转为 `FAILED`、释放活动记录且禁止进入统计与奖励，解决损坏轨迹永久阻塞新运动的问题。
- [x] 开始前检查定位可用性、定位精度、用户完整档案/体重和同一用户未完成记录。
- [x] 前台持续采集 GCJ-02 定位点，客户端做采样和即时展示过滤，服务端再按精度、时间顺序、定位间隔、位移和异常速度校验。
- [x] 实时展示时长、有效距离、平均配速和估算卡路里；批次确认和结束时以服务端结果覆盖客户端估算。
- [x] 本地暂存轨迹、每 10 点自动批量上传、序号断点恢复、批次幂等和结束幂等代码已完成。
- [x] 服务端复算并保存运动摘要、批次和轨迹点，结束时返回最终距离、配速和卡路里。

已完成代码接口：`POST /app/workouts`、`GET /app/workouts/active`、`GET /app/workouts/{id}`、`POST /app/workouts/{id}/track-batches`、`POST /app/workouts/{id}/pause`、`POST /app/workouts/{id}/resume`、`POST /app/workouts/{id}/finish`、`POST /app/workouts/{id}/abandon`。

首版跑步卡路里采用 `体重(kg) × 距离(km) × 1.036`，并保存算法版本 `RUNNING_WEIGHT_DISTANCE_V1`。该数值是可解释的运动估算，不是医疗指标。客户端显示值包含尚未上传点的即时估算；服务端过滤后的结果才是最终事实。

当前持续定位只使用 `startLocationUpdate + onLocationChange` 的前台能力，离开页面会停止监听，回到页面后恢复。前后台持续定位需要额外的平台权限、隐私说明和真机验收，当前不得对外宣称已经支持锁屏/后台持续跑步。

验收：开始、暂停、继续、结束、杀进程恢复、弱网补传、重复结束、漂移点和超速点都有验证。

### 阶段 4：运动历史与个人统计

- [x] 运动记录数据库分页列表、单次详情和有效轨迹分段加载/地图回放代码。
- [x] 直接从已完成运动聚合累计卡路里、距离、次数和时长，避免加载全部记录到 JVM。
- [x] 统计最长连续运动天数、城市覆盖数，并提供近 7/30/90 个自然日的真实卡路里、距离、时长和次数趋势；已通过 MySQL 8 查询验证，登录态接口仍待业务账号联调。
- [x] 非跑步运动采用运动类型 + 时长 + 档案体重 + MET/规则版本计算卡路里；手工记录计入个人累计、趋势和成就，首版不参与地区排行榜。
- [x] 跑步距离、配速、跑步卡路里和按时长 MET 卡路里公式已抽为无外部依赖的可审计计算器，并覆盖正常值、边界值和坐标距离单元测试；数据库聚合统计仍直接来源于可追溯运动事实。

已完成代码接口：`GET /app/workouts`、`GET /app/workouts/{id}`、`GET /app/workouts/{id}/track-points`、`GET /app/statistics/overview`。轨迹接口每次最多返回 500 个服务端判定有效的点，前端按序号继续加载；列表每页最多 50 条，统计使用数据库聚合，均避免一次性读取用户全部运动/轨迹。

已完成代码接口：`GET /app/statistics/trends?days=7`，仅接受 7/30/90 天并由服务端补齐无运动日期；`GET /app/sport-types` 返回数据库启用的按时长运动；`POST /app/workouts/manual` 幂等保存非跑步运动并由服务端结算。

当前按时长支持健走、骑行、跳绳、羽毛球、力量训练和瑜伽，规则来自 `run_sport_type`，每次运动保存运动名、体重、MET 与算法版本快照。公式为 `MET × 体重(kg) × 时长(小时)`，算法版本为 `MET_DURATION_V1`；这些值是可解释估算，不是医疗指标。

### 阶段 5：文化线路、城市足迹与成就

- [x] 参考 `lupao/` 的京杭大运河、万里长城、长征视觉线路，采用数据库运营配置和真实接口页面，而非前端 Mock；尚未执行 SQL/运行接口。
- [x] 运动开始时快照当前线路，结束时将有效里程累计到对应虚拟线路；线路切换只影响后续运动。
- [x] 达到线路节点后返回解锁状态；线路推进和成就判定使用运动 ID/唯一键与事务保证重复结束不会重复发放。
- [x] 城市足迹在运动结束事务提交后异步生成：最多抽取 24 个服务端判定有效的轨迹点，经腾讯逆地址解析后按城市行政区划码去重并聚合；失败状态、重试次数和下次重试时间均持久化，尚未执行 SQL/调用腾讯接口验证。
- [x] 支持累计卡路里、距离、次数三类基础里程碑及解锁时间。
- [x] 连续天数、城市数里程碑；城市类成就在异步足迹事务完成后判定，避免运动结束时城市尚未解析而漏发。
- [x] 正式奖励积分：每日签到普通 +20，连续第 7 天起每日 +40；线路节点和成就按运营配置奖励，以业务唯一键防止重复发放。

已完成代码接口：`GET /app/routes`、`GET /app/routes/{id}`、`PUT /app/routes/current`、`GET /app/achievements`、`GET /app/city-footprints`、`POST /app/city-footprints/retry`、`GET /app/points`、`POST /app/points/check-in`。积分流水只由服务端业务事件写入，客户端不能直接指定积分数量。

### 阶段 6：排行榜

- [x] 城市榜、省榜代码及页面支持日/周/月/累计周期；尚未执行 SQL/运行接口。
- [x] 默认核心指标为有效累计卡路里，响应明确地区、周期起点、生成时间和卡路里/距离/次数/用户 ID 的稳定排序规则。
- [x] 排行地区读取用户档案常住地，不使用运动发生城市。
- [x] 用户归属地变更规则：v5 将常住省市冻结到每次运动；档案修改只影响之后创建的运动，不搬动历史榜单成绩。迁移前历史记录按执行 v5 时的档案地区一次性补录。
- [x] MySQL 从有效运动事实计算 Top 100 与本人精确名次，Redis 缓存地区周期榜单 60 秒；缓存可由数据库重建。

已完成代码接口：`GET /app/rankings?scope=CITY&period=WEEK`。

验收：跨地区、并列、异常运动剔除、周期切换、缓存重建和本人名次均正确。

### 阶段 7：视觉还原、质量与发布准备

- [x] 创建绑定 `shuaibo888/running` 的 Codex Cloud 环境 `燃赛路跑-running`：Universal 镜像固定 Java 17，缓存 Maven 依赖并执行后端全模块构建；补充根目录 `AGENTS.md` 约束云端开发和验证边界。
- [ ] 按 `lupao/` 逐页对照首页、跑、线路详情、运动中、我的、成就、积分/激励页面；首页、我的、积分及三栏导航已完成首轮正式还原，其余页面继续统一细节。
- [ ] 将 Mock 图和占位数据替换为合法、压缩、可配置的正式资源和真实接口；当前首页、我的页已全部接真实统计/线路/成就/积分接口，复用的样稿图片已复制到 `front/static/running/`，正式发布前仍需确认素材版权及压缩策略。
- [ ] 控制主包/分包大小，增加骨架屏、空态、错误态和无权限状态。
- [ ] 后端单元/集成测试，前端关键交互测试，微信开发者工具和真机验收。
- [ ] 配置生产 HTTPS、微信合法域名、隐私协议、腾讯地图额度告警和服务端密钥。

## 6. 初步数据模型

- `run_user_profile`：平台用户运动资料、卡路里计算资料、排行榜归属地；不直接保存或返回微信身份标识。
- `run_wechat_identity`：平台用户与微信身份摘要的服务端映射；当前不保存原始 `openid`，若以后引入订阅消息，需另行设计密钥托管下的可逆加密凭据表。
- `run_phone_identity`：平台用户与手机号摘要的服务端映射；原始手机号只在发送和校验短信的当前请求中使用。
- `run_sport_type`：运动类型、MET/卡路里规则与版本。
- `run_workout`：单次运动摘要、状态、结算值、异常标识及运动创建时的排行榜省市快照。
- `run_track_batch` / `run_track_point`：轨迹批次与轨迹点；后续根据数据量决定压缩存储策略。
- `run_user_stat`：用户累计统计。
- `run_workout_city`：单次有效运动解析出的城市结果，按运动和城市唯一，保留当次行政区划快照。
- `run_city_footprint`：用户有效运动城市聚合及首次/最近达成记录。
- `run_virtual_route`、`run_virtual_route_node`、`run_user_route_progress`：文化线路与节点进度。
- `run_achievement`、`run_user_achievement`：成就定义与解锁记录。
- `run_point_account`、`run_point_transaction`：积分余额、连续签到和带业务唯一键的可审计积分流水。

`v1` 至 `v7` 已在本地 MySQL 按顺序执行并视为不可变迁移；后续数据库变化只能从 `v8` 开始新增版本文件。v6 建立 `run_schema_history` 迁移登记表，今后执行任何新版本前必须先查询已登记版本并核对历史脚本哈希。v7 为运动档案增加头像 OSS 记录关联。现有表仍须随真实数据量继续审查索引、幂等约束和审计策略。

## 7. 关键验收原则

- 静态页面完成 ≠ 功能完成。
- 前端显示成功 ≠ 后端业务成功，必须核对 HTTP 状态和 JSON `code=200`。
- 编译通过 ≠ 服务已运行；接口必须进行真实请求验证。
- 模拟器定位通过 ≠ 真机持续定位通过。
- 轨迹上传成功 ≠ 运动有效；必须经过服务端校验和结算。
- SQL 文件存在 ≠ 数据库已迁移；迁移必须记录实际执行结果。

## 8. 当前下一步

1. 在 IDEA 重新运行一次最新后端，重新获取验证码验证手机号登录；已确认短信送达和验证码校验成功，旧故障来自 `SysUser → SysUserVo` 转换器缺失，代码已改为直接读取用户实体。
2. 登录后验证 `GET /app/statistics/overview`、`GET /app/statistics/trends`、`GET /app/achievements`、`GET /app/points` 和签到接口，并核对重复签到、成就/线路奖励的幂等流水。
3. 在微信开发者工具查看新版首页、三栏导航、我的、运动记录、非跑步运动和积分页面；HBuilderX 已自动产出对应 WXML/JS/WXSS，仍需模拟器视觉验收。
4. 用真实已完成 GPS 运动验证腾讯地图异步城市解析、失败重试、幂等和足迹聚合，再验证真机定位、弱网补传和完整跑步闭环。
5. 登录后验证头像选择、5MB/格式/像素限制、对象存储上传和档案刷新；在微信开发者工具核对新增协议页面的滚动、双文档切换及“查看协议不会误勾选、登录必须主动勾选”，再继续逐页完成 `lupao` 视觉还原。

## 9. 进度记录

| 日期 | 阶段 | 结果 | 验证 | 遗留问题 |
| --- | --- | --- | --- | --- |
| 2026-09-03 | 基线 | 建立长期目标、详细实施步骤和腾讯地图方案 | 文档检查 | 微信/腾讯地图密钥未申请，业务尚未实现 |
| 2026-09-03 | 阶段 0 | Ping 真实连通；增加前后端分环境配置和本地配置说明 | `/app/ping` 返回 `code=200`、`status=UP` | 小程序界面结果仍待开发者工具确认；生产域名和密钥未配置 |
| 2026-09-03 | 阶段 0/1 | 新增 `ruoyi-running` 模块、`run_user_profile` 档案模型、受保护的查询/保存接口及首版 SQL | JDK 17 执行 `mvn -pl ruoyi-admin -am -DskipTests compile`，33 个 Reactor 模块全部 `SUCCESS` | SQL 未执行；微信登录未实现，因此档案接口尚未做数据库和登录态运行验证 |
| 2026-09-03 | 阶段 1 | 新增微信登录/退出、身份摘要映射、令牌签发、前端 `uni.login` 与授权请求封装；SQL 扩展为档案和身份两张表 | 调整事务边界后再次完成 JDK 17 全 Reactor 编译，33 个模块全部 `SUCCESS` | 缺真实 AppSecret；SQL 未执行；HBuilderX 编译、微信首次/再次登录和档案接口仍待运行验证 |
| 2026-09-03 | 需求修订 | 登录明确为微信一键登录、手机号验证码登录两个正式入口，并补充双身份绑定与冲突保护规则 | 交接文档和阶段 1 清单已同步 | 手机号验证码接口、短信供应商配置和身份表尚未实现 |
| 2026-09-03 | 阶段 1 | 实现短信验证码发送、手机号登录、微信/手机号双向绑定、冲突保护、统一令牌服务及 `run_phone_identity` | JDK 17 全 Reactor 编译，33 个模块全部 `SUCCESS` | SQL 未执行；短信供应商未配置，短信发送、验证码重放和双身份绑定尚无运行证据 |
| 2026-09-03 | 阶段 1 | 身份摘要升级为服务端密钥 HMAC-SHA-256；新增双入口登录页、60 秒验证码倒计时、协议确认和退出入口；鉴权头按 Sa-Token 要求补齐 `Bearer` 前缀 | JDK 17 再次执行全 Reactor 编译，33 个模块全部 `SUCCESS`；`git diff --check` 通过 | HBuilderX/微信开发者工具尚未编译登录页；SQL、微信和短信仍待真实联调 |
| 2026-09-03 | 阶段 1 | 新增运动档案编辑页、绑定状态查询、微信/手机号绑定入口；“未知性别”不再错误地标记为档案完整 | 获准访问 D 盘 Maven 本地仓库后，JDK 17 全 Reactor 编译 33 个模块全部 `SUCCESS` | 数据库 SQL 尚未执行；前端页面、真实身份绑定和档案保存尚无运行证据 |
| 2026-09-03 | 阶段 2 | 新增微信定位权限声明、单次 GCJ-02 定位页、地图展示、权限拒绝恢复入口和腾讯逆地址解析后端代理；增加限流、超时与 Redis 短缓存 | JDK 17 全 Reactor 编译 33 个模块全部 `SUCCESS`；实现依据 uni-app 定位文档与腾讯 WebService API 路径核对 | 腾讯 WebService Key 未配置；HBuilderX 编译、开发者工具模拟定位和真机定位均待验证 |
| 2026-09-03 | 阶段 3 | 新增运动、轨迹批次、轨迹点三张表；实现开始/暂停/继续/结束状态机、服务端轨迹过滤与结算；新增参考 `lupao` 信息层级的腾讯地图跑步页、前台定位、本地暂存、批量补传和恢复逻辑 | JDK 17 执行 `mvn -pl ruoyi-admin -am -DskipTests compile`，33 个 Reactor 模块全部 `SUCCESS`；前端 JS/JSON 静态语法和 `git diff --check` 通过 | SQL 未执行；HBuilderX、模拟定位、弱网/杀进程、真机轨迹均未运行；后台持续定位未接入 |
| 2026-09-03 | 阶段 4（部分） | 新增已完成运动数据库分页、个人累计聚合、有效轨迹按 500 点分段读取；新增运动历史和详情轨迹回放页面 | JDK 17 全 Reactor 再次编译 33 个模块全部 `SUCCESS`；新增页面脚本与页面配置静态检查通过 | SQL/接口未运行；连续天数、趋势、非跑步运动与城市统计未实现；大量轨迹真机渲染性能待验证 |
| 2026-09-03 | 阶段 5/6（部分） | 新增三条文化线路、节点进度、次数/距离/卡路里成就，以及城市/省份日周月累计排行榜前后端代码 | JDK 17 全 Reactor 编译 33 个模块全部 `SUCCESS`；页面脚本静态检查通过 | SQL/接口未运行；城市足迹、连续天数/城市成就、地区变更审计待实现 |
| 2026-09-03 | 需求修订/阶段 1 | 微信一键登录改为官方手机号授权组件，同一请求提交微信身份 code 与 phoneCode；平台账号统一按已验证手机号归并，移除手工绑定入口 | JDK 17 全 Reactor 编译 33 个模块全部 `SUCCESS`；登录页、档案页和认证模块脚本静态检查通过 | 需认证小程序主体、真实 AppID/AppSecret、微信手机号能力和真机授权完成运行验证；短信供应商仍未联调 |
| 2026-09-03 | 阶段 5（城市足迹支点） | 新增运动提交后异步抽样解析、单次运动城市快照、用户城市足迹聚合、失败退避/定时重试和手工重试接口，并新增正式城市足迹页 | JDK 17 全 Reactor 编译 33 个模块全部 `SUCCESS`；前端脚本/页面配置静态检查和 `git diff --check` 通过 | 13 表 SQL 未执行；腾讯 Key、真实逆地址响应、定时重试、接口和 HBuilderX 页面均无运行证据；目标已按用户要求暂停于此 |
| 2026-09-04 | 配置/迁移规则 | 开发期数据库、Redis、微信、短信、腾讯地图配置改为工程内明文；明确已执行迁移不可修改，新增 `20260904_running_v2.sql` | `/app/ping` 返回 `code=200`；本地 MySQL 核对 13 张 `run_*` 表；仅执行 v2 后核对 4 条新增成就 | 微信 AppSecret、短信和腾讯地图 Key 仍待负责人填写；部署前必须迁回安全配置并轮换真实密钥 |
| 2026-09-04 | 阶段 4/5 | 新增 7/30/90 日运动趋势、最长连续运动天数和城市覆盖统计；新增连续 3/7 天与 3/10 城成就，城市成就在足迹解析事务完成后判定；运动记录页增加真实趋势图 | JDK 17 全 Reactor 33 模块编译和打包成功；MySQL 8 事务测试得到 4 个趋势日、最长连续 3 天、2 城，回滚后临时数据为 0；HBuilderX 自动生成最新微信小程序产物；首次重启确认新路由无令牌返回 `code=401` | 修正 `ONLY_FULL_GROUP_BY` 后需在 IDEA 再次启动最新类；缺真实业务登录令牌，接口数据响应和微信模拟器视觉仍待验证；非跑步运动和正式积分未实现 |
| 2026-09-04 | 阶段 1 缺陷修复 | 真实短信验证码已送达且校验通过；定位登录失败为框架缺少 `SysUser → SysUserVo` 运行期转换器，手机号和微信登录统一改为直接读取 `SysUser` 后签发令牌 | 异常栈精确落在 `RunPhoneAuthServiceImpl.resolveOrCreateUser`；JDK 17 全 Reactor 33 模块 `BUILD SUCCESS`；编译产物反汇编确认调用 `SysUserMapper.selectById`，身份映射关联 1 个启用中的 APP 用户 | 当前 8080 进程启动早于修复编译，需 IDEA 重启并重新获取验证码完成真实登录复验 |
| 2026-09-04 | 阶段 4 非跑步运动 | 新增可运营运动类型、MET/时长结算、手工运动页面、历史/详情适配和排行榜公平性隔离；新增且仅执行 `20260904_running_v3.sql` | 本地 MySQL 核对 7 种规则和 7 个新/调整字段；事务测试 30 分钟健走按 60kg 结算 114.00 kcal、个人统计包含、排行榜计数为 0，回滚后临时记录为 0；HBuilderX 已生成 `manual-workout` 小程序产物 | 需登录后跑通真实接口与页面；MET 值后续应由产品/运动专业人员复核，正式积分仍未实现 |
| 2026-09-04 | 阶段 1/7 协议与隐私 | 新增橙色品牌视觉的用户协议/隐私政策阅读页，披露手机号归并、档案、轨迹定位、算法估算、第三方处理和上线前待补事项；登录页协议名称可分别打开且不会误切换同意状态 | `pages.json`/`manifest.json` JSON 静态解析、Vue 脚本抽取语法检查及 `git diff --check` 通过 | HBuilderX/微信开发者工具尚未渲染；运营主体、联系方式、保存期限、注销/信息申请入口和法律审核仍是上线阻塞，当前文本不得冒充已通过平台审核 |
| 2026-09-05 | 阶段 1 登录失效闭环 | 新增统一会话失效处理，普通请求与头像上传遇到 HTTP/业务 401 时同时清除令牌和当前账号未上传轨迹，并对并发 401 只提示、跳转一次；主动退出复用同一清理逻辑，登录成功重置跳转门闩 | Node 内置测试 3 项通过，覆盖账号本地数据清理、并发 401 合并和重新登录后再次失效；Vue/JS 语法检查与 `git diff --check` 通过 | 微信开发者工具及真机仍需验证 Toast、reLaunch 和运动页销毁时序；后端真实 Token 过期依赖本地运行环境 |
| 2026-09-05 | 阶段 3 失败恢复 | 为无法继续或结束的活动运动增加放弃接口与前端二次确认入口；服务端按用户加锁校验所有权，重复放弃幂等返回，完成记录禁止放弃，失败记录释放活动唯一约束且不参与排行、统计、线路、成就或积分 | 全部 Vue 脚本与 JSON 静态检查、`git diff --check` 通过；代码路径审计确认放弃操作不发布完成事件、不调用奖励服务且不上传待处理轨迹 | Maven 仍受华为镜像依赖 403 阻塞；需本地 MySQL 验证 RUNNING/PAUSED/重复/越权/已完成五类接口场景，并在微信工具验证确认框和恢复采集时序 |
| 2026-09-04 | 阶段 5 正式积分 | 基于 `lupao` 积分页完成积分账户、每日签到、连续签到加成、成就/线路节点奖励、业务幂等流水和真实积分页面；接入 `GET /app/points`、`POST /app/points/check-in` | 负责人确认 v4 已手工执行；数据库核对两张积分表、成就奖励值；JDK 17 全 Reactor 33 模块 `BUILD SUCCESS`；HBuilderX 自动生成 points 页面产物 | 8080 仍需重启到最新编译类后，用真实登录态验证签到及奖励流水 |
| 2026-09-04 | 阶段 6 地区审计 | 新增且仅执行 `20260904_running_v5.sql`，在运动创建时冻结排行榜省市，查询改为使用运动快照，前端展示地区归属规则 | v5 执行成功；真实 MySQL 核对 4 个快照字段和城市/省份两条复合索引；事务测试把档案从保定改到石家庄后，旧运动仍只命中保定快照，回滚后临时数据为 0；v1-v4 SHA-256 前后不变；JDK 17 全 Reactor 33 模块 `BUILD SUCCESS` | 需重启后通过真实登录态接口验证榜单响应；Redis 榜单缓存最多延迟 60 秒 |
| 2026-09-04 | 阶段 7 首页/我的首轮还原 | 参考 `lupao` 将开发调试首页替换为真实数据首页，新增首页/跑/我的 tabBar、真实个人中心、积分/成就/线路入口；样稿资源复制到正式静态目录但不依赖 `lupao` 路径 | HBuilderX 自动生成最新 `index`、`mine` 和 `app.json`，产物确认包含三栏 tabBar 与 mine 路由；`lupao/` 仍由 `.gitignore` 命中 | 当前电脑控制服务未向任务暴露原生应用窗口，无法截取微信模拟器进行视觉验收；素材发布权与压缩仍待确认 |
| 2026-09-04 | 迁移治理 v6 | 新增且仅执行 `20260904_running_v6.sql`，建立 `run_schema_history`，登记 v1-v6 并保存 v1-v5 SHA-256，后续运行 SQL 前不再依赖口头状态 | v6 执行成功；真实 MySQL 返回 v1-v6 六条登记；v6 文件 SHA-256 为 `C4BC90970ACDF7C436D1B9939DF2C633EF965C01F8CBA2B4C68E80877678988D` | v6 是创建登记表的引导迁移，库内 checksum 留空并由库外哈希核对；下一迁移必须从 v7 开始 |
| 2026-09-04 | Cloud 续作准备 | 在 Codex 设置中创建并验证 `燃赛路跑-running` 环境，绑定私有仓库 `shuaibo888/running`；固定 Java 17，配置 Maven 首次全模块构建、依赖维护缓存及非敏感构建变量；创建并发布专属分支 `codex/cloud-running`，Cloud 只可在该分支直接提交推送 | Codex 环境详情页显示“创建环境成功”；`git push -u origin codex/cloud-running` 成功并建立远端跟踪；根目录 `AGENTS.md` 已强制禁止 Cloud 触碰 `main`、强推、擅自开 PR/合并/发布 | 本地明文配置必须继续留在工作区、不进入 Git。Cloud 无法运行 HBuilderX/微信开发者工具；负责人已另行授权仅在云端分支提交一次 `lupao/` 冻结快照，供源码级视觉对照但不能替代真机验收，后续严禁提交该目录更新 |
| 2026-09-04 | 阶段 1 头像上传 | 新增 `POST /app/user/profile/avatar`，校验图片扩展名、MIME、真实图片头、5MB 大小和 4096 像素边界后上传现有 OSS；v7 保存 `avatar_oss_id`，前端相册/相机选择后直接刷新档案，普通档案保存不再接受任意头像 URL | JDK 17 全 Reactor 33 模块静默编译退出码 0；v7 仅执行一次，真实 MySQL 返回 v7 登记、`avatar_oss_id bigint` 和 `(tenant_id, avatar_oss_id)` 索引；v7 SHA-256 为 `8F1278DF26FD0E09A3779A5A7003BD03AC1975B85BCE47C61A0F5665E827A92E`；HBuilderX 自动生成含 `chooseMedia`/上传逻辑的 profile 产物 | 需重启最新后端，用真实登录态及已配置 OSS 验证成功上传、非法图片拒绝和微信合法域名；下一迁移必须从 v8 开始 |
| 2026-09-04 | Cloud 分支语义修正 | 确认网页选中 `codex/cloud-running` 后，任务容器内显示 `work` 且无 `origin` 是平台隔离机制；任务不得再因该现象误停 | 首次云任务环境使用 Java 17 完成 37 个 Maven Reactor 模块，全部 `SUCCESS`；停止前未修改、提交或推送任何业务文件 | 新任务需从已含 `lupao/` 一次性冻结快照的最新 `codex/cloud-running` 启动；正式开发仍只写入 `front/`、`back/` 和文档，严禁后续更新 `lupao/` |
| 2026-09-04 | 阶段 4/7 结算自动化测试 | 将跑步距离、配速、跑步卡路里和 MET 卡路里公式抽为无框架依赖的可审计计算器，业务结算统一调用；新增正常值、无效输入、短距离和北京坐标距离边界测试 | `git diff --check` 通过；计算器由 JDK 17 独立编译通过；Maven 测试因 Cloud 配置的华为镜像返回 403 且公共 Central DNS 不可用而无法完成依赖解析 | 网络恢复后执行 `mvn -B -ntp -pl ruoyi-modules/ruoyi-running -am -DskipTests=false -Dtest=RunWorkoutCalculatorTest -Dsurefire.failIfNoSpecifiedTests=false test`，预期 4 个测试全部通过；随后继续补充状态机与幂等服务测试 |
